# Cài đặt:  pip install -r requirements.txt
# Bật venv:  .\Chatbot\Scripts\Activate.ps1   (PowerShell)

# --- Parsing ---
pypdf==6.10.2
pdfplumber==0.11.9
beautifulsoup4==4.14.3
lxml==6.1.0
pdfminer.six==20251230   # transitive của pdfplumber, pin để khoá
python-docx>=1.1.0       # parse .docx (văn bản dự thảo / Bộ Tư pháp)

# --- Retrieval ---
rank-bm25>=0.2.2         # BM25 hybrid + RRF fusion
duckduckgo-search>=6.0.0 # web_search tool (không cần API key)

# --- Text processing ---
regex==2026.4.4
Unidecode==1.4.0

# --- Schema / config ---
pydantic==2.13.3
python-dotenv==1.2.2

# --- Utils ---
tqdm==4.67.3
loguru==0.7.3

# --- Chunking ---
langchain-text-splitters>=0.2.0

# --- Embedding (nặng ~3GB, chỉ cài khi chạy bước embedding) ---
sentence-transformers>=3.0.0
torch>=2.2.0
FlagEmbedding>=1.2.10  # for BGE-M3

# --- Vector DB ---
chromadb>=0.5.0
# qdrant-client>=1.9.0  # bật khi chuyển sang prod

# --- LLM ---
anthropic>=0.40.0
google-genai>=2.0.0
groq>=1.0.0
openai>=1.30.0  # cho 9Router OpenAI-compatible client

# --- API server (kết nối chatbot-ui / open-webui) ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9   # upload file qua API

# --- UI demo ---
streamlit>=1.36.0

# --- Knowledge Graph ---
neo4j>=6.0.0
pyvis>=0.3.2          # visualize graph trong Streamlit

# --- OpenSearch backend (VECTOR_BACKEND=opensearch) ---
opensearch-py>=2.4.0
pyarrow>=15.0.0       # export/ingest embeddings qua parquet

# --- Module 2: Legal NER (phân tích bản án) — service riêng port 8100 ---
# Chạy:  cd legal_ner && uvicorn api.main:app --port 8100
PyMuPDF>=1.24          # fitz: đọc text-layer PDF (extractor=pymupdf, mặc định trên Windows)
transformers>=4.46     # token-classification (xlm-roberta-base) cho NER
pikepdf>=8.0           # verify/forgery.py — phân tích metadata PDF (Layer 4 forgery)
easyocr>=1.7           # OCR bản án scan/ảnh (api/ocr_easyocr.py); kéo theo torchvision
# (tùy chọn) sentence-transformers đã có sẵn ở trên — dùng cho charge-match tiebreaker
# Hai gói dưới CHỈ cần khi TRAIN/EVAL lại model NER, bỏ qua nếu chỉ chạy inference/API:
datasets>=2.19         # training.train
seqeval>=1.2           # training.evaluate
